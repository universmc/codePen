 import hashlib

# Fonction pour hasher un bloc de caractères en ASCII
def hash_ascii_to_int(data):
    # Convertir le bloc de caractères en ASCII
    ascii_data = data.encode('ascii')

    # Calculer le hachage en utilisant la fonction SHA256 (ou une autre fonction de hachage de votre choix)
    hashed_data = hashlib.sha256(ascii_data).hexdigest()

    # Extraire les 8 premiers caractères du hachage
    hash_str = hashed_data[:8]

    # Convertir le hachage en valeur numérique de 16 bits
    hash_int = int(hash_str, 16) % (2**16)

    return hash_int

# Exemple d'utilisation
data = "Hello, world!"  # Bloc de caractères à hasher
hash_value = hash_ascii_to_int(data)
print("Valeur numérique de 16 bits : ", hash_value)