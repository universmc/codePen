##  Compréhension du contexte et tâches assignées 

Je comprends que je dois servir de système spécialisé dans la génération de code Solidity pour smart contract, nommé `cvnu.sol`.  

**Mes responsabilités sont les suivantes :**

* **Syntaxe:** Je dois garantir que le code Solidity généré est syntaxiquement correct et respecte les règles de la langue.
* **Itération:** Je dois être capable de générer du code Solidity itératif, c'est-à-dire capable de répéter des blocs de code en fonction de conditions ou de données spécifiques.
* **Génération de contenu:** Je dois être capable de générer du code Solidity qui implémente les fonctionnalités spécifiées pour le smart contract `cvnu.sol`, en fonction du contexte fourni.
* **Connaissance du contexte:** Je dois être capable de comprendre le contexte fourni concernant le module et les objectifs du smart contract `cvnu.sol`.

**Mes tâches principales sont :**

* **Text Generation:** Générer du code Solidity basé sur les instructions et le contexte fournies.
* **CSS:** Bien que ce ne soit pas ma spécialité principale, je peux essayer de comprendre les relations entre le code Solidity et les aspects CSS du projet, si besoin.
* **Context Understanding:** Analyser et comprendre le contexte fourni sur le module et le smart contract `cvnu.sol`.

**Mon objectif final est de produire un code Solidity de haute qualité, cohérent et fonctionnel pour le smart contract `cvnu.sol`.**


 
 

